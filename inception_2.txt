openssl req -x509 -nodes -out /etc/nginx/ssl/inception.crt -keyout /etc/nginx/ssl/inception.key -subj "/C=FR/ST=IDF/L=Paris/O=42/OU=42/CN=login.42.fr/UID=login"

CN for Common name
TLS: Transport Layer Security, layer 4 of the OSI model (Open System Interconnection)

Nginx
	conf file: nginx.conf in /usr/local/nginx/conf or /etc/nginx or usr/local/etc/nginx
	nginx -s stop/quit/reload/reopen
	log files: access.log and error.log in /usr/local/nginx/logs or /var/log/nginx
	
Preparation de la VM
	# apt-get update && apt-get upgrade
	#redimensionnement de la fenetre
	> su
	> apt-get install build-essential module-assistant
	> m-a prepare
	> mount/media/cdrom
	> sh /media/cdrom/VBoxLinuxAdditions.run
	(restart)
	
	#mettre rertzer dans groupe sudo
	> su
	> usermod -aG sudo rertzer
	
	#install vim
	> apt-get install vim
	
	#install docker
	> sudo apt-get update
	> sudo apt-get install ca-certificates curl gnupg
	> sudo install -m 0755 -d /etc/apt/keyrings
	> curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
	> sudo chmod a+r /etc/apt/keyrings/docker.gpg
	> echo \
  		"deb [arch="$(dpkg --print-architecture)" signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
  		"$(. /etc/os-release && echo "$VERSION_CODENAME")" stable" | \
  	> sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
  	> sudo apt-get update
  	> sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
  	# on teste que ca marche:
  	> sudo docker run hello-world
  	# s'ajouter au groupe docker
  	> usermod -aG docker rertzer
  	
  	#ajouter cle vogosphere
  	> cd ~/.ssh
  	> ssh-keygen -t rsa
  	
  build une image:
  	docker build . -t nginx
